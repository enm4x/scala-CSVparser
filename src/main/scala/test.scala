/*
import csv.CsvReader

object test extends App {

  def main(): Unit ={
  /*
   * Test unitaire effet de bord pour la fonction addElementToList
   *
   * error testing :
   */
    val errTypeWrong = addElementToList(ListedCar, person0)
    val errList = addElementToList(ListedFilm, car0)
    val errTypeInt = addElementToList(ListedCar, 4)
    val errType = addElementToList(ListedCar, cat0)

    /* good case testing */
    val insertcar = new car("ford", "france", 200, 7)
    val car1 = addElementToList(ListedCar, insertcar)
    println("ma nouvelle liste car 1 :" , car1)
    val cat1 = addElementToList(ListedCat, cat0)
    val film1 =addElementToList(ListedFilm, film0)
    val act1 =addElementToList(ListedActor, actor0)
    val person1 = addElementToList(ListedPerson, person0)
  }

}
*/
